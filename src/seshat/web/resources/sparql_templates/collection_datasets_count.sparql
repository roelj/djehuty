{% extends "prefixes.sparql" %}
{% block query %}
SELECT (COUNT(DISTINCT ?dataset) AS ?datasets)

WHERE {
  GRAPH <{{state_graph}}> {
    <{{collection_uri}}> rdf:type                         shst:Collection ;
                         shst:datasets/rdf:rest*/rdf:first ?dataset .
  }
}
{% endblock %}
