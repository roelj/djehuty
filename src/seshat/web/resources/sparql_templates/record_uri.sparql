{% extends "prefixes.sparql" %}
{% block query %}
SELECT DISTINCT ?uri
WHERE {
  GRAPH <{{state_graph}}> {
    ?uri     rdf:type     shst:{{record_type}} ;
             shst:{{identifier_name}} {{identifier}} .
  }
}
{% endblock %}
