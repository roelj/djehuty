{% extends "prefixes.sparql" %}
{% block query %}
SELECT DISTINCT ?account_uuid ?draft ?doi ?latest_published_version ?first_online_date
       ?totals_created_at ?total_downloads ?total_views ?total_shares ?total_cites
       ?latest_published_version_number ?container_uuid

WHERE {
  GRAPH <{{state_graph}}> {
    ?container            rdf:type                      shst:{{item_type.capitalize()}}Container .
    OPTIONAL { ?container shst:account                  ?account .           }
    OPTIONAL { ?container shst:draft                    ?draft .             }
    OPTIONAL { ?container shst:doi                      ?doi .               }
    OPTIONAL { ?container shst:first_online_date        ?first_online_date . }
    OPTIONAL { ?container shst:totals_created_at        ?totals_created_at . }
    OPTIONAL { ?container shst:total_downloads          ?total_downloads .   }
    OPTIONAL { ?container shst:total_views              ?total_views .       }
    OPTIONAL { ?container shst:total_shares             ?total_shares .      }
    OPTIONAL { ?container shst:total_cites              ?total_cites .       }
    OPTIONAL { ?container shst:latest_published_version ?latest_published_version .
               OPTIONAL { ?latest_published_version shst:version   ?latest_published_version_number . }
             }

    BIND(STRAFTER(STR(?account), "account:") AS ?account_uuid)
    BIND(STRAFTER(STR(?container), "container:") AS ?container_uuid)
  }
  FILTER(?container = <container:{{container_uuid}}>)
}
{% endblock %}
