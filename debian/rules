#!/usr/bin/make -f
%:
	dh ${@} --buildsystem=autoconf --with=python3

override_dh_auto_install:
	dh_auto_install ${@}
	install -D debian/seshat/usr/local/bin/seshat debian/seshat/usr/bin/seshat
	rm -r debian/seshat/usr/local/bin
